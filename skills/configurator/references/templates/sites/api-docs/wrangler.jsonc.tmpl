{
  "name": "{{PROJECT_NAME}}-api-docs",
  "main": "src/worker.ts",
  "compatibility_date": "2024-12-01",
  "routes": [
    {
      "pattern": "api.{{DOMAIN}}",
      "custom_domain": true
    }
  ],
  "vars": {
    "API_BACKEND_URL": "{{API_BACKEND_URL}}",
    "PROJECT_NAME": "{{PROJECT_NAME}}",
    "DISPLAY_NAME": "{{DISPLAY_NAME}}"
  }
}
