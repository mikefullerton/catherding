# Git Workflow

You MUST commit after each logical change. Do not accumulate uncommitted work.

You MUST push after every commit. The remote branch MUST always reflect current state.

You MUST create a draft PR before writing any code.

## MUST NOT

- Do not commit directly to the main branch.
- Do not accumulate uncommitted changes.
- Do not skip the draft PR step.
