{
  "name": "{{PROJECT_NAME}}-main",
  "main": "src/worker.ts",
  "compatibility_date": "2024-12-01",
  "assets": {
    "directory": "dist",
    "binding": "ASSETS"
  },
  "routes": [
    {
      "pattern": "{{DOMAIN}}",
      "custom_domain": true
    }
  ],
  "vars": {
    "API_BACKEND_URL": "{{API_BACKEND_URL}}"
  }
}
