import { useAdminAuth } from "../context/auth";

export function DashboardPage() {
  const { user } = useAdminAuth();

  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">Admin Dashboard</h1>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
        <a href="/users" className="p-6 bg-white border rounded-lg hover:shadow-md transition">
          <h2 className="font-semibold text-lg">Users</h2>
          <p className="text-sm text-gray-500 mt-1">Manage accounts and roles</p>
        </a>
        <a href="/flags" className="p-6 bg-white border rounded-lg hover:shadow-md transition">
          <h2 className="font-semibold text-lg">Feature Flags</h2>
          <p className="text-sm text-gray-500 mt-1">Toggle features on/off</p>
        </a>
        <a href="/messaging" className="p-6 bg-white border rounded-lg hover:shadow-md transition">
          <h2 className="font-semibold text-lg">Messaging</h2>
          <p className="text-sm text-gray-500 mt-1">Send and track messages</p>
        </a>
        <a href="/feedback" className="p-6 bg-white border rounded-lg hover:shadow-md transition">
          <h2 className="font-semibold text-lg">Feedback</h2>
          <p className="text-sm text-gray-500 mt-1">Review user submissions</p>
        </a>
      </div>
      <p className="mt-8 text-sm text-gray-400">Logged in as {user?.email}</p>
    </div>
  );
}
