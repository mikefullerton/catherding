{
  "name": "{{PACKAGE_SCOPE}}/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "{{PACKAGE_SCOPE}}/shared": "file:../shared"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.30.2",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^4.76.0"
  }
}
